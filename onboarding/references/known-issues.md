# Known issues register — tools & disrupt-sc

Protocol (RUNBOOK.md standing rule 2): when a phase hits one of these, apply the recorded
resolution if one exists; when a NEW issue surfaces, ask the user (fix tool / use as-is /
defer) and append it here with a new KI id + the decision.

Status: `open` = no decision yet · `workaround` = agreed workaround, tool unchanged ·
`fixed` = patched locally · `wontfix` = accepted behavior.

| ID | Component | Issue | Status | Resolution / workaround |
|---|---|---|---|---|
| KI-01 | disrupt-sc `validate_inputs.py:25` | Warns "no user-defined config" for every scope using `.local.yaml` — it only checks the committed `.yaml`, never `.local.yaml`. | open | Ignore the spurious warning; candidate one-line fix. |
| KI-02 | disrupt-sc `init_pipeline/transport.py` | Duplicate-edge-ID warning tells the user to "re-run build_transport.py", but that script (in disrupt-sc-data) builds multimodal connectors, not ids; unique ids actually come from `osm-extractor finalize`. Misleading message. | open | Follow failure-triage instead of the message; candidate message fix. |
| KI-03 | disrupt-sc `config/default.yaml` + docs | Dead `filepaths` keys documented but never read: `usd_per_ton` (read from sector_table column instead), `inventory_duration_targets` (`definition: inputed` unimplemented), `admin`. | open | Don't set them in new scope configs. |
| KI-04 | disrupt-sc docs | Stale pages: `user-guide/input-data.md` describes v1 per-mode `*_edges.geojson` + an outdated sector_table schema; `quick-start.md` advertises removed `--input_coverage` (now `--flow_coverage`); `MRIO_SPECIFICATION.md` orphaned from mkdocs nav; `AGENTS.md` references deleted `main.py`. | open | Trust loader code / tool-recipes.md over these pages. |
| KI-05 | mrio-extractor | No CLI/config-file interface: selections are edit-in-place constants, so past extractions are unreproducible (the Gulf selection is already lost). | workaround | Snapshot the CONFIGURATION block into the scope manifest after every run (RUNBOOK rule 5). Candidate feature: add a `--config` YAML entry point. |
| KI-06 | mrio-extractor | No Eora support (no script, no data on disk), despite Eora appearing in the informal MRIO quality ranking. GLORIA is the actual 4th database. | open | Use GLORIA as the wide-coverage fallback; decide separately if Eora is ever needed. |
| KI-07 | osm-extractor | No `airways` mode (exists only as an enum in multitnbuild) and no PBF download/clip step. | workaround | Airways: hand-build great-circle links between main airports. Download: `scripts/geofabrik.py`. |
| KI-08 | tnclean `cli.py:127-155` | After `--config` is loaded, every CLI option is re-assigned unconditionally, so YAML `cluster_*` (and most other) values are silently overwritten by CLI defaults; `tnclean config` doesn't even emit cluster keys. | workaround | Always pass cluster flags on the command line, never via `--config`. Candidate fix: guard config overrides like `metric_crs`. |
| KI-09 | tnclean | Rich progress bars crash on Windows cp1252 consoles (`UnicodeEncodeError`). | workaround | Always `--no-progress` (documented in the tool's CLAUDE.md). |
| KI-10 | tnclean DBSCAN | Cluster contraction chains transitively: `cluster_max_extent` far exceeds `eps` (56 km at eps=5 km on Moldova). Not a bug per se, but silently erases geography. | workaround | Always read `cluster_max_extent` in the report; reduce eps or use `--cluster-position medoid` where structure matters. |
| KI-11 | disrupt-sc `mrio.py:447` | `_adjust_output` silently inflates exports when input > output for a region_sector (logged, easy to miss). Large adjustments distort trade shares. | open | Check the log line count after load; investigate if N is large. |
| KI-12 | disrupt-sc label detection `mrio.py:434` | Row/column classification by regex means innocuous sector names (`Private Households`, `Taxes on products`, anything with `va`) silently corrupt the matrix layout. | workaround | Reserved-word check enforced by `scripts/check_scope.py` in phase 4. |
| KI-13 | osm-extractor `cli.py` | Rich console summary crashes on Windows cp1252 consoles (`UnicodeEncodeError` on `→`/diacritics) AFTER the gpkg is written — the JSON report is lost, exit looks clean through a pipe. Same disease as KI-09. | fixed | Patched 2026-08-27 (Romania onboarding): stdout/stderr reconfigured to UTF-8 `errors=replace` on win32 at CLI import. Committed locally in osm-extractor. Belt-and-braces: run toolchain with `PYTHONIOENCODING=utf-8`. |
| KI-14 | onboarding `scripts/build_spatial.py` firms-default | Sector columns seeded with `pd.NA` become object dtype, so GeoJSON serializes populations as strings; DisruptSC's `importance > 0.0` filter then raises `TypeError: '>' not supported between str and float` (agents.py:814). | fixed | Patched 2026-08-27 (Romania onboarding): cast sector columns via `pd.to_numeric` before writing. Symptom if regenerating with an old script: run dies at firm creation with that TypeError. |
| KI-15 | disrupt-sc `run.py:506` + `config.py:271` | `--open` silently no-ops when `export_files: False` (the default.yaml default): `setup_output` returns None, so the `if export_folder and open_report` gate skips report generation with no warning — run looks successful but nothing opens. | fixed | Patched 2026-08-27: `--open` now forces `export_files: True` in `_main_impl`, and `execute` warns when `open_report` is set but no export folder exists (criticality / Monte Carlo). Committed locally in disrupt-sc. |
| KI-17 | disrupt-sc `init_pipeline/agents.py` (`_integrate_spatial_firms`) | The per-point values of firms.geojson (population, plant capacity, tonnage) were discarded: MRIO output was split EQUALLY across a region_sector's points, so supplier choice was distance-only and plant sizes could not matter. | fixed | Patched 2026-08-31 (user decision "fix now"): weights normalized within each region_sector scale the output proportionally; valueless files keep equal split. Changes firm-size heterogeneity in every scope with a wide firms.geojson — re-validate before comparing runs across commit bda9641. |
| KI-18 | data: CEADS refinery inventory (`Firms/CEADS/*.xls`) | `Latitude`/`Longtitude` columns are swapped (value ranges prove it), and the "Production (t)" unit contradicts the workbook's own NOTE sheet — global sums match barrels/day, ~50x off t/yr. | open | Un-swap + verify units against Lei et al. 2021 (One Earth) inside the future CEADS adapter; never read the raw file directly. |
| KI-19 | data: GID-Power (`Firms/GID/`) | No coordinates (plant name + country only), variable is CO2 not capacity/generation, and the WEPP (S&P commercial) lineage makes redistribution risky. | workaround | Decision 2026-08-31: adopt GEM power trackers (MW + coordinates + CC-BY) as the power source; keep GID only for 1990–2022 time-series work. Implemented as the `gem_power` adapter over the 8 per-technology trackers (the merged GIPT file was never needed); Romania validation: 639 units / 20.8 GW ≈ national total. |
| KI-20 | data: Maus et al. mining polygons (`Firms/MausEtAl/`) | Only the PANGAEA metadata stub was downloaded — all six payload files (polygons gpkg, 3 rasters, country csv, validation gpkg) are missing. Licence is CC-BY-**SA** (copyleft) with a commercial S&P sampling frame. | open | Fetch from PANGAEA 942325 if mining area-weights are needed as a Jasansky fallback; mind the ShareAlike licence on derivatives. |
| KI-16 | disrupt-sc `mrio.py:195` (`get_tech_coef_dict_for_selection`) | Firms never received imported inputs: the tech-coef builder read suppliers from `get_intermediary()`, which excludes `imports` rows, so no firm `input_mix` ever contained a country input and the firm→country "import" link path (`supply_chain.py:228`) was dead code. Firms were insensitive to import disruption; Romania was missing $79.6B/yr of intermediate imports. Found in phase-8 validation (imports frozen at the FD-column value regardless of flow_coverage). | fixed | Patched 2026-08-27 (user decision "fix now"): import rows concatenated into the tech-coef supplier matrix. Downstream machinery (`_identify_suppliers` import case, equilibrium `import_weights`, Country.deliver) was already in place. All prior scope runs predate firm imports — re-validate before comparing. |
| KI-21 | firm-extractor `sources/climatetrace.py` `_fetch` | An API error/empty payload (a dict such as `{"bbox": [0,0,0,0]}` for a subsector with no assets, e.g. `other-chemicals` on the EU) was `extend`ed into the asset list, so its KEYS became string "assets", got CACHED, and the loader crashed later with `'str' object has no attribute 'get'`. | fixed | Patched 2026-09-03 (EU onboarding): non-list payloads raise (logged as a skipped subsector, never cached); non-dict assets are skipped. Delete any `_cache/*_<subsector>_*.json` whose elements are strings. |
| KI-22 | firm-extractor `sources/regional_stats.py` + `scripts/eurostat_nuts3_employment.py` | Multi-country scopes: the CSV needs an `iso3` column (adapter raises), and Eurostat NUTS3 LABELS never match GISCO polygon NAMES for EL/BG (transliteration) or renamed units; also several countries publish only the coarse service groups (G-J, K-N, O-U) at NUTS3 - a single fine-group map silently leaves those countries' services on the fallback. | fixed | 2026-09-03: fetcher takes a comma-separated country list and `--geo-codes` (NUTS code as admin_name); `eurostat_nuts_households.py admin --name code` writes polygons keyed by code; the EU build chooses fine vs coarse service groups per country (>= 90% unit coverage) before expanding and sums to NUTS2. Switzerland has no nama_10r_3empers rows -> CHE dispersed sectors fall back to population. |
| KI-23 | firm-extractor `gem_power` on large scopes | Every operating unit becomes a firm point: 36,087 EU units (each solar/wind farm) would create 36k sector-D firms. | workaround | EU 2026-09-03: capacity summed to NUTS2 (`power_mw_nuts2.csv`) and fed through `regional_stats`; candidate tool feature: `gem_power` option `aggregate_to: admin` / `min_mw`. |
| KI-24 | disrupt-sc `config.py` `build_params` + `config/default.yaml` | `sectors_to_exclude: None` (unquoted) is the STRING "None" in YAML; `tuple("None")` = ('N','o','n','e'), so every ICIO-native scope silently lost sector **N** (administrative & support services): Romania runs report "49 sectors" of 50 and "dropped 1 region_sectors"; the EU smoke run dropped 28. GLORIA-named scopes were unaffected (no one-letter sector called N). | fixed | 2026-09-03: `_parse_sector_list` accepts None/"None"/"" as empty and rejects other bare strings; default.yaml now `[]`. Re-run Romania (and any ICIO scope) before quoting sector-N-sensitive results; totals move by the size of sector N (~4-8 % of services output). |
| KI-25 | disrupt-sc `network/transport_network.py` route cache | `cache_route`/`retrieve_cached_route` canonicalised reversed routes with `copy.deepcopy` + `revert()`: deepcopy of a Route (list subclass with pickle hooks) doubled the list part and cost ~90 of the ~130 build minutes on the EU scope (763k routes). | fixed | 2026-09-03 (7363ad4): `Route.reversed_copy()` rebuilds from the four stored fields; results identical. |
| KI-26 | disrupt-sc `init_pipeline/routing.py` `_precompute_and_assign` | Kept the single-source Dijkstra path to EVERY reachable node for every source (~8M path lists on the EU scope) → MemoryError on a 32 GB machine while the links need ~200k keys. | fixed | 2026-09-03 (44e90dd): keep only the destinations each source ships to. |
| KI-27 | disrupt-sc `init_pipeline/routing.py` capacity-constrained routing | The hub-based candidate heuristic runs per OD-cargo group, single-threaded, no progress logging: on the EU scope (786k links → 197k groups) it had not finished generating candidates after 81 CPU-minutes. `capacity_constraint: gradual|binary` is unusable above ~50k links. | superseded 21 Sep 2026 (KI-40) | Keep capacity routing off on continental scopes; represent capacity loss with closures + `transport_cost_shock` (c641fa5) and calibrate port choice by cost (per-mode `cost_of_time`). |
| KI-28 | disrupt-sc `init_pipeline/agents.py` agent placement | Firms/households snapped to the nearest node of ANY mode; on a trunk-only network (TEN-T) 46 % of EU firm output sat on rail nodes and 12 % on water/sea nodes, shipping with no access leg or transfer cost, so rail/barge were structurally too cheap and transfer-cost calibration was inert. | fixed | 2026-09-03 (9eb15df): `agent_attachment: roads` (default `any` keeps existing scopes unchanged). Set it for every TEN-T/trunk-only scope. |
| KI-29 | disrupt-sc `critical_input_threshold` default 0.0 (strict Leontief) on large multi-region scopes | With sector-resolved imports and 28 regions x 50 sectors the EU scope carries thousands of negligible cross-border service inputs (e.g. 0.04 mUSD/week of Belgian postal services in a 707 mUSD/week German retailer, 7-day stock). Under strict Leontief every kept input binds, so one faltering micro-supplier zeroes big firms and the cascade spreads within weeks, unrelated to the shock (EU 2026 scenario run 1: EU production −1.45 % by week 3 from service firms). | workaround | Set `critical_input_threshold` to a cost-share proxy (EU: 0.02) or load a PBL criticality matrix (Pichler et al. 2022). The single-step initial_state run cannot reveal it — run a few undisrupted steps (`simulation_type: disruption` with no disruptions) before any scenario on a new scope. |
| KI-30 | disrupt-sc `run.py` cache path + `network/route.py` | Initialisation memory on the EU scope: the supply-chain pickle expands to ~6 GB (786k `CommercialLink` + networkx graph); with all caches current `run.py` loaded it, set initial conditions (~10 min), then loaded the routes pickle which carries a second copy plus the `Route` objects (~7 kB each: list subclass with duplicated tuple lists; reverse-direction retrievals build a private copy per link). Peak 23 GB private on a 31 GB machine -> page-file thrashing, step 0 never finished. | fixed | 5e09946 skips the supply-chain stage and the pre-routing equilibrium when the routes cache is current (every simulation entry point resets initial conditions itself); 369a8aa gives `Route` `__slots__` and a single copy of its tuples, and `intern_routes()` shares one Route per node sequence between links, route plans and the library (fresh builds at the end of `setup_logistic_routes`; older caches are interned on load and re-saved once); the commercial-link table is dropped after caching. Profile script: `mem_profile_stages.py` pattern (RSS per stage + object census). Keep run outputs outside OneDrive. |
| KI-31 | disrupt-sc `agents/transport_utils.py` `send_shipment` (cost-shock/closure ceiling) + `run_pipeline/simulate.py` routing summary | The substitution ceiling scales `link.delivery` in place, so the exported link_data cannot separate tonnage withheld by the ceiling from supplier rationing (`delivery == realized_delivery` on every link); `blocked_usd` in routing_summary.csv (served_order − realized_delivery) mixes the two as well. On the EU Rhine run the direct effect (~16 bn USD) had to be reconstructed from the baseline Kaub link value × withheld share while the model-wide shortfall was 1,754 bn. | fixed 21 Sep 2026 | `link.delivery_offered` (the quantity decided before the ceiling and the capacity gate) is exported in link_data.csv, so rationed = `served_order - delivery_offered` and withheld by transport = `delivery_offered - realized_delivery`; routing_summary.csv reports `capacity_blocked_usd` (the gate's part of `blocked_usd`) on its own. |
| KI-32 | disrupt-sc `init_pipeline/supply_chain.py` `_aggregate_import_inputs` + `init_pipeline/agents.py` `load_inventories` | Sector-resolved import needs are folded into one `{BLOC}_imports` input per partner when the supply chain is built, AFTER `load_inventories` set per-input targets on the agents, so every imported input fell back to ONE time step of stock (firm.py `inventory_duration_target.get(input_id, 1)`). On the EU Rhine scenario a single closed week zeroed the power plants' coal and the road hauliers' fuel (both imported bundles), and the cascade that followed was mistaken for a propagation problem. The criticality matrix has the same blind spot: `imports` is not a sector, so import bundles default to critical. | fixed | `import_bundle_shares(mrio)` recovers each bundle's sector composition from the MRIO import rows of the buyer's column; `load_input_criticality` and `load_inventories` give `{BLOC}_imports` keys the share-weighted mean criticality and inventory duration of that composition (fallback: critical / the `imports` or default duration); run.py re-applies both once the input mix is final, whichever stages came from cache. EU check: DEU refinery imports from RUS = 73 % crude + 20 % refined products. |
| KI-33 | disrupt-sc `run_pipeline/export.py` `AgentWriters` + `run.py` (`export_files`) | The per-step exports are all-or-nothing: `link_data.csv` grows to 4.6-6.3 GB per EU scenario run (786k links x 23-30 weeks) and `inventory_data.csv` to 2-2.8 GB, while the analyses need only the firm/household files, `routing_summary.csv` and the weekly flows; ten runs filled drive C and a run died silently at week 21 of 23 (a full disk also swallows the traceback and the redirected log). | open | Add per-file switches (e.g. `export_link_data`, `export_inventory_data`, default on) or a sampled link export before the sensitivity grid; keep 10 GB free per queued run; monitor free disk space in the run watcher. |
| KI-34 | disrupt-sc `init_pipeline/agents.py` `_integrate_spatial_firms` (and the internal-cell firm rows) | The firm table was assembled by iterating a Python `set` of region-sector strings, whose order follows the process's string hash seed: every process built a differently ORDERED firm table, hence different firm ids and positional names, hence different RNG draws in the supply-chain build — `seed 42` did not pin the realisation. Found 11 Sep 2026 when a cluster batch on the EU scope (caches rebuilt there) did not reproduce the laptop baseline (German loss 2.67 % vs 0.90 % of a quarter on the same profile; two builds with PYTHONHASHSEED 0 and 1 shared zero identical firm rows, supplier sets of same-named firms overlapped 17 %). The cached laptop world was one draw; fresh draws differ from it by up to 3x in the disruption response because the fuel channel runs through ~85 liquid-bulk source nodes. | fixed 11 Sep 2026, verified | `sorted(...)` in `_integrate_spatial_firms` and the internal-cell rows, a sorted `input_mix` at construction (agents.py), sorted iteration of the four draw-consuming loops of `supply_chain.py` (`input_mix`, `qty_purchased`, `sector_consumption`, `transit_from`), sorted `transport_modes` in the link table (routing.py). Verified with two isolated builds under PYTHONHASHSEED 0 and 1: identical firm tables, identical 1,380,040 supply-chain links and weights, identical production in weeks 0-1. Any result computed before the fix is a single unreproducible draw: rebuild the caches and report ensembles over seeds. Rule for new code: never iterate a set of strings into an ordered structure. |
| KI-35 | disrupt-sc `agents/transport_utils.py` `discover_route` + `network/commercial_link.py` `_routes_have_modal_switch` | The alternative-route search minimised transport cost alone and the switching penalty was only applied afterwards by the give-up rule, so a shipper with a prohibitive modal-switch penalty (bulk) was sent to the cheapest path even when that path introduced a new mode, and then gave up - although a detour on its own modes existed and would have passed. On the EU Rhine scope any surcharge on the Lower Rhine made a rail leg the cheapest alternative for ~860 Kaub-crossing bulk links in closure weeks, so the surcharge acted by presence only (factor 1/3 = factor 1, factor 0 different) and the closure losses roughly doubled (2026: 2.74 % vs 1.35 % of a quarter). The switch test also counted a DROPPED mode as a switch. Found 14 Sep 2026 by replaying week 6 of a laptop pair with full link exports. | fixed and verified 14–15 Sep 2026 (line-haul rule + per-cargo line-haul modes; ten-week pairs `lrcheck_*_lh`, `lrcheck_*_lhb`) | `discover_route(..., switching_costs)` searches two candidates - the shipper's own modes with its access modes weighted by 1 + penalty in the search (cache 'alternative_same_modes:...') and free - and returns the cheaper including the penalty as a fraction of the normal bill. Modal switch = LINE-HAUL RULE: the alternative puts more than `access_km` (50) extra kilometres on a mode that carried less than `line_haul_km` (100) in the normal route (both under `logistics.switching_costs`); more km on a line-haul mode (a canal detour) or a longer access leg is not a switch. Neither set equality (a 200 km road leg passed) nor a 'no new mode' rule (an all-road path passed, since every route has road access legs) separates access from line haul. Tests: `tests/test_penalty_aware_alternative.py`. Verification (EU Rhine, first 10 weeks): an offline classification of the 2,831 Kaub-crossing bulk OD pairs in the model's functions predicts the run link by link (1,553 pairs give up, 1,278 deliver); German closure-week losses 28–33 % below the penalty-blind code. Two network facts decide the outcome and are calibration choices, not code: the road bypass of the closed Kaub reach is 88 km (a switch only because it exceeds the 50 km allowance; at 90 km almost all Kaub bulk delivers) and 35 % of the Kaub-crossing bulk routes already carry 100 km or more of road, so they may lengthen that leg without limit (median +206 km). The factor-0 twin reproduces the line-haul base to the decimal (15 Sep): the surcharge acts through prices only. Since 15 Sep (user decision, 8d26c6c) `line_haul_modes` names the modes that may carry line haul per cargo class - road is never one for bulk, so a bulk shipper may not lengthen a road leg beyond the allowance whatever its length (containers keep the km rule; a shipper with no eligible mode on its normal route keeps the km rule on its own modes); verified on the same pair: 75 % of the week-6 Kaub bulk given up, predicted link by link, ten-week German loss 0.40 % of a quarter against 0.55 % under the penalty-blind code; its factor-0 twin identical to the decimal. Sensitivity table in `disrupt-sc-data/EU/calibration_log.md` (14 Sep). Every result before the fix that involved closures with a surcharge elsewhere on the route carries the artefact. |
| KI-36 | disrupt-sc `studies/rhine2026/run_rhine.py` `adjust_inventory_targets` | The stock multiplier `--inventory-scale` followed the sector list of `--inventory-add-days`: with a list, only the listed buyers were scaled (and shifted), every other buyer kept the unscaled targets. A targeted-stock lever on a calibrated buffer therefore ran most of the economy uncalibrated (16 Sep 2026 cluster run `2026_cal_stock7t`: +29 % instead of a saving; its log says `default 30 d` against `default 60 d` in the base). Found by comparing the run's 'inventory targets adjusted' log line with the base's. | fixed 16 Sep 2026 | The multiplier applies to every buyer, the shift to the listed sectors only; the wrong run is kept as `2026_cal_stock7t_wrongscale`, the lever rerun on the laptop. Lesson: a run whose log prints the adjusted `default` days tells at once whether the calibration is on. |
| KI-37 | disrupt-sc `network/transport_network.py` `_calculate_cost_per_ton` | The time term of the edge cost was multiplied by `time_factor[time_resolution] / 7` (1/7 at daily, 1 at weekly, 4.35 at monthly resolution). `cost_of_time` is USD per ton-hour and the travel time is km / speed in hours, so hours x cost_of_time is already the per-ton cost of the trip; nothing else in the edge cost (rates per tkm, fees) is per step, so the factor changed the weight of time against distance and with it route and mode choice, the give-up rule and the delivered-price pass-through. On the EU parameters a 500 km container leg cost road/rail/barge 26/18/13 USD/t at daily, 33/27/26 at weekly and 61/64/74 at monthly resolution - the barge cheapest at daily, dearest at monthly, for the same trip. The v1 code (June 2025) priced a trip as total time x cost_of_time with no factor; the factor arrived with the March 2026 rewrite (2ceaac7, `adjusted_delay_cost`) without a comment. Reported 16 Sep 2026 by a student whose costs are in EUR per tonne-hour and who had removed the factor in their fork. | fixed 16 Sep 2026 (user decision) | Factor removed; `_calculate_cost_per_ton` and `ingest_logistic_data` no longer take `time_resolution` (capacities in tons per step are still converted in `init_pipeline/transport.py`). Retro-compatibility: weekly scopes are bit-identical (EU/Rhine, Romania, Gulf, Armenia, China, Italia, TEN-T, Global2, World, Testkistan, macroMIP), so the Rhine value-of-time calibration stands; daily scopes (Cambodia, ECA) now value time 7x more than before - divide every `cost_of_time` entry by 7 to reproduce old runs exactly; monthly scopes 4.35x less (Ecuador with transport on - multiply by 4.35; the earthquake ensemble runs with transport off and is unaffected). Stage cache versions bumped (`_STAGE_BUILD_VERSION`: transport_network 2, logistic_routes 3) so networks and routes cached before the fix are refused and rebuilt. Tests: `tests/test_edge_cost_resolution_independent.py` (same edge costs at daily and monthly resolution on Testkistan while capacities scale x30; time term = hours x USD per ton-hour). |
| KI-38 | disrupt-sc `run_pipeline/simulate.py` `set_initial_conditions` + `agents/firm.py` `initialize_finance` | The equilibrium cost decomposition subtracted a transport line `eq_production x transport_share` on top of the input cost, but `transport_share` (`Mrio.get_transport_input_share`) is the transport-sector rows of the same MRIO column divided by total intermediate inputs, and those rows are already inside the Leontief matrix W (every kept input row becomes a supplier link; all 140 EU and the single Ecuador transport region-sectors survive flow coverage), so the same MRIO cells were counted twice - and inflated by output / intermediate inputs (median 1.7x EU, 2.0x Ecuador): 7.6 % of output re-subtracted in the EU against 4.1 % of true transport purchases (Ecuador 9.0 % vs 3.3 %). The transport line was never updated during a run (v1's `record_transport_cost` has no v2 counterpart), so profit was unaffected (the 'other' residual absorbed it), but (a) the value added behind `capital_initial` = ratio x (sales - inputs - transport) was 0.84 of MRIO value added in both scopes, 0.11 for EU transport sectors, and 73 EU region-sectors got VA <= 0, i.e. zero modeled capital (no capital destruction, no idle-capital mobilization); (b) the "negative equilibrium 'other' cost" warning fired on ~8,400 of 12,700 firms in every EU run (Romania too). Inherited verbatim from v1 (`tmp/v1/model.py:616`). Found 15 Sep 2026 while reviewing `initialize_finance`. | fixed 15 Sep 2026 (user decision: drop the transport term, size capital on sales minus inputs) | Costs are the intermediate inputs (transport sectors included) plus the 'other' residual that closes the target margin; `initialize_finance(eq_input_cost, eq_other_cost, periods_per_year)`; value added = sales - inputs everywhere (`build.firm_va_shares`, `agents.report_inventory_to_gdp` and `studies/earthquake/paper/diagnose_flow.py` carried the same subtraction). `eq_profit` and run-time profit are unchanged (transport + other only ever entered as their constant sum); capital stocks rise to ratio x MRIO value added (EU +19 % in aggregate, transport sectors x9; Ecuador +18 %, mining +38 %, trade x2), the 73 EU region-sectors regain modeled capital, and the warning now flags only true margin/VA inconsistencies (EU: 324 region-sectors whose tax/subsidy or VA rows do not close the column; Ecuador 1). Every Rhine run before 15 Sep carries the old stocks: it matters only through idle-capital mobilization of the firms that had none (transport, warehousing), not through prices or profit. The Ecuador earthquake calibration (absolute destroyed capital split in proportion to `capital_initial`) was done on the old stocks and must be re-checked. `transport_share` itself (price pass-through, give-up rule) is untouched; it is transport purchases over intermediate inputs, not over output (1.7-2x larger) - a separate decision. 139 tests pass; Testkistan `initial_state` runs. |
| KI-39 | disrupt-sc `agents/transport_utils.py` `send_shipment` + `network/transport_network.py` (`update_load_on_route`, `_capacity_multiplier`, `_refresh_edge_capacity_costs`) | A transport capacity never bound on a shipper's MAIN route: the congestion label `cost_per_ton_with_capacity` was read only by displaced shippers (closure / cost-shock branches), it was refreshed AFTER each placement against the step's partial load in agent delivery order (first come first served: countries, then firms in dict order; a capped bypass ended at 149 % of capacity and the late 11.5 kt household link was dropped in full while a 1.5 kt firm link took the free room), loads reset every step, and the `gradual` multiplier gave an empty capacitated edge HALF its base cost (0.5 at zero load, 1.0 at 80 % utilisation), so the give-up rule and the pass-through compared a discounted detour with an undiscounted normal bill. Probe (`scripts/capacity_probe_testkistan.py`, 21 Sep 2026): Testkistan trunk capped at 10 % of its baseline load, 1000 % of capacity placed, deliveries bit-identical to capacity off in both modes. | fixed 21 Sep 2026 (user decision: capacity rework, `docs/architecture/transport-capacity.md`) | Within-step capacity gate (`run_pipeline/capacity_gate.py`, step 7g): after every agent has shipped, a saturated edge cuts the shipments of the current round proportionally, earlier rounds keep their allocation (round priority), the saturated edge is excluded from the step's later searches, each cut share is re-sent through the penalty-aware search with the switching penalty and give-up rules, the residue returns to the supplier's stock as `capacity_blocked`; at most |C| + 1 rounds. On-off: no cost label depends on load. `capacity_constraint: gradual|binary` raise; the old code is on `legacy/v2-capacity-routing`. |
| KI-40 | disrupt-sc `init_pipeline/routing.py` capacity-aware initial assignment (`heuristic`, `lp`, `edge_lp`) | The hub-candidate generator ran two networkx Dijkstras per OD-cargo group whatever the capacities (KI-27: 197k groups on the EU scope, killed after 81 CPU-min), placed chunks in dict order, ignored `capacity_routing_max_iterations` and `*_candidate_overlap`, and both LPs were global optimisation; a shared capacity of 0 crashed `_find_congested_edges`; the post-solve labels were hard-coded `gradual`; six helpers had no caller. | fixed 21 Sep 2026 (retired) | One route per link from the batched scipy Dijkstra whatever `capacity_constraint`; with it on, `baseline_capacity_check.csv` and a warning per capacitated edge whose baseline load exceeds its capacity (the gate rations it from t = 0). Multi-route plans (`route_plan`, `_send_chunked_shipment`) retired with it: they ignored cost shocks and the closure substitution share. routing.py 2081 -> 489 lines. |
| KI-41 | disrupt-sc `init_pipeline/transport.py` (`_apply_default_capacities`, `_apply_capacity_overrides`, `_load_transport_edges`) | GeoPackage `capacity` / `capacity_<cargo>` columns were read, scaled, then overwritten by `default_transport_capacity` in every branch (the DISCARDED warning recommended a fix that would also overwrite them; `input-data.md` documented the column); an override NAME matching no edge was silently ignored; the unlimited sentinel `1e9 x time_factor` against the `< 1e8` tests made an edge capacitated or not depending on `time_resolution` (5 Mt/day maritime: finite weekly, unlimited monthly). | fixed 21 Sep 2026 | Columns dropped with an info line; an edge has a capacity only if named (no sentinel: a missing key is no capacity); unknown names, unknown cargo types and negative values raise, also from `validate-inputs`; `default_transport_capacity` replaced by `cargo_mode_eligibility` (its zeros: airways containers only, pipelines liquid bulk only), the per-mode placeholder capacities dropped so the gated set is exactly the named edges. |
| KI-42 | disrupt-sc `agents/transport_utils.py` `_discover` (alternative-route cache) + `network/transport_network.py` `place_shipment` | A cached alternative route was reused across steps without checking that its edges were still open, so a route found under one closure set could cross an edge closed since (the Rhine runs close the same edge week after week and never met the case); the substitution-ceiling split placed its main and alternative parts under the same edge key, so a shared edge kept one of them in the flow export (loads were right). | fixed 21 Sep 2026 | A cached route is reused only while `is_route_available` on the current network; every part of a delivery carries its own edge key (`<pid>__alt`, `<pid>__g<round>` for the gate's re-sends) and the destination accumulates them under the link pid. |
